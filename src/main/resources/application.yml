server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/odb?serverTimezone=Asia/Seoul
    username: ott-project
    password: ott1234!

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
